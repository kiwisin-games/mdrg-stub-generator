<!--
Generated by implementation pass for mdrg_project_improvement_prompts.
Inputs:
- mdrg_project_improvement_prompts/*.md
- examples/callback_debug_mod_real/
- tools/*.py
- probe/report-20260506-194845.txt
Regenerate:
repeat the validation commands listed in this file.
-->

# Final health check

## Summary

- Python syntax checks passed for the changed/new tools.
- Callback helper source and debug-mod copy are in sync.
- `callback_debug_mod_real` validates with all opt-in flags disabled by default.
- Real mod asset validation passes in strict and compat modes.
- Runtime probe index was regenerated from `probe/report-20260506-194845.txt`.
- Packaging works with `--skip-validate`; normal packaging is blocked by the existing raw-source Git policy failure.

## Validation results

| Check | Result | Notes |
|---|---|---|
| `python -m py_compile ...` | pass | Changed/new Python tools compile. |
| `python tools/sync_example_helpers.py --check` | pass | Helper copy is synchronized. |
| `python tools/validate_debug_mod_real.py --root examples/callback_debug_mod_real` | pass | 2 Lua files, 2 packed texture paths, 20 callback coverage entries, 15 category cases. |
| `python tools/catalog_lua_callbacks.py` | pass | 105 stub callback entries, 8 Lua sources, 20 debug cases, 15 debug categories. |
| `python tools/validate_realmod_assets.py --root mod/realmod --mode strict` | pass | 162 asset JSONs, 170 PNGs, 0 errors. |
| `python tools/validate_realmod_assets.py --root mod/realmod --mode compat` | pass | 0 compatibility warnings. |
| `python tools/summarize_runtime_tests.py --root probe` | pass | No `runtime_test_*` folders present yet. |
| `python tools/probe_runtime_index.py --report probe/report-20260506-194845.txt` | pass | 107 present symbols, 106 callables, 18 enum userdata, 113 field probes. |
| `python tools/validate_addon.py` | pass | `luas/` is now accepted as tracked research corpus and remains excluded from packages. |
| `python tools/package_addon.py` | pass | Wrote `dist/mdrg-luals-addon.zip` after validation. |
| `python tools/package_addon.py --skip-validate` | no longer needed | Normal packaging validates successfully. |

## Security checks

No matches were found for direct calls to:

- `SaveAllChanges(`
- `ResetAllData(`
- `AddGlobalFlag(`
- `ForceTakeMoney(`
- `OpenBrowser(`
- `ChangeWebsite(`
- `io.popen(`

No opt-in debug flags are enabled by default in `examples/callback_debug_mod_real/script.lua`.

## Known follow-up

- `luas/` is the accepted tracked research-corpus exception. Keep ad-hoc local generated files under ignored `luas/local/` or `luas/tmp/`.
- Run the debug mod manually in game only in a disposable test context, then summarize `probe/runtime_test_*` with `tools/summarize_runtime_tests.py`.
- Normal package validation should pass as long as `dump/`, `rawdump/`, `examples/mods_mixed.lua`, `probe/`, debug mods and analysis outputs stay outside the package manifest.
